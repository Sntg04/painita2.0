{
  "name": "painita",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "npm run build && node backend/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
